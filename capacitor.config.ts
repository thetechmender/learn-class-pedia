import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.learnclasspedia.app',
  appName: 'Learn Class Pedia',
  webDir: 'dist/learn-class-pedia/browser',
};

export default config;
